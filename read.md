📄 web.md: 발주서 ➡️ 송장 변환 웹 시스템 구축 기획서
1. 💡 핵심 아키텍처 (Deep Thinking: 외부 유출 원천 차단 전략)
웹 서비스에서 데이터 유출을 막는 가장 완벽한 방법은 **"서버로 데이터를 보내지 않는 것"**입니다.
기존에는 Python 서버(백엔드)로 엑셀 파일을 업로드하여 처리하는 방식이 일반적이었으나, 최신 웹 기술을 활용하면 사용자의 브라우저 메모리 내에서만 엑셀을 읽고, 화면에 띄우고, 다시 엑셀로 저장할 수 있습니다.

Local-First Web Architecture 적용: * 사용자가 웹페이지에 엑셀 파일을 드래그 앤 드롭해도, 이 파일은 서버로 전송(Upload)되지 않습니다.

브라우저 단(Client-side)에서 JavaScript가 즉시 엑셀 데이터를 파싱합니다.

따라서 네트워크 패킷을 가로채거나 서버가 해킹당하더라도 **고객사 주소, 연락처 등 민감한 개인정보가 유출될 가능성이 0%**입니다.

2. 🛠 추천 기술 스택 (Tech Stack)
확장성과 웹 기반 UI/UX를 고려하여 다음과 같은 프론트엔드 중심의 스택을 제안합니다.

프레임워크: React.js 또는 Vue.js (추후 기능 확장을 위한 컴포넌트 기반 UI 구성)

엑셀 파싱 및 생성: SheetJS (xlsx) 또는 ExcelJS (브라우저 내에서 엑셀 파일을 완벽하게 읽고 쓰는 표준 라이브러리)

데이터 검증 그리드 (Table): AG Grid 또는 TanStack Table (수천 건의 엑셀 데이터를 엑셀 프로그램처럼 웹에서 부드럽게 렌더링하고 정렬/필터링 제공)

스타일링: Tailwind CSS (빠르고 깔끔한 비즈니스 대시보드 UI 구축)

3. 🚀 구체적인 개발 단계 (Step-by-Step Guide)
Step 1. 안전한 파일 입력 인터페이스 구축 (Dropzone)
기존 ui.py의 드래그 앤 드롭 영역을 웹으로 이관합니다.

UI 구성: 화면 중앙에 넓은 점선 박스(Dropzone)를 배치합니다.

보안 로직 적용: <input type="file" accept=".xlsx, .xls">를 사용하되, form 태그의 action(서버 전송) 속성을 절대 사용하지 않고, JavaScript의 FileReader API를 통해 브라우저 메모리로만 파일을 읽어 들입니다.

Step 2. 파서 로직(Parser.py)의 JavaScript 이관
기존 Python openpyxl로 작성된 동적 헤더 탐색 로직을 SheetJS 기반으로 변환합니다.

1차 탐색 (메타데이터): 1~50행을 순회하며 'TEL', '담당자', '납품처 주소'를 정규식과 키워드로 추출합니다.

2차 탐색 (데이터 테이블 위치 파악): 'NO.' 또는 'NO'가 있는 셀을 찾아 데이터 시작점(start_row)을 동적으로 파악하고, '제품명', '바코드', '총 주문수량'의 열(Column) 인덱스를 식별합니다.

3차 추출 및 매핑: 확인된 start_row부터 순회하며 기존 OUT_HEADERS (28개 항목: 순번, 거래처코드, 출하의뢰번호 등) 포맷에 맞춘 JSON 배열 데이터로 재조립합니다.

Step 3. 사용자 웹 화면 검증 (Data Grid View)
사용자가 변환 결과를 다운로드하기 전, 웹 화면에서 직접 눈으로 확인하는 단계입니다.

그리드 렌더링: Step 2에서 생성된 JSON 배열을 AG Grid 컴포넌트에 주입합니다.

시각적 체크: * 수량이 0이거나 데이터가 누락된 셀은 붉은색 배경으로 하이라이트 처리하여 사용자 실수를 방지합니다.

기존 데스크톱 앱의 QTableWidget이 제공했던 정렬 기능을 웹 그리드에 기본 적용합니다.

수정 기능 (선택): 기존에는 수정이 불가능(NoEditTriggers)했지만, 웹에서는 필요 시 특정 셀(예: 배송메시지)을 더블클릭해 바로 수정할 수 있는 기능을 추가해 업무 효율을 높일 수 있습니다.

Step 4. 보안 엑셀 내보내기 (Export to Excel)
동적 파일명 생성: 현재 날짜(YYYYMMDD)를 결합해 송장임시_가차_202XMMDD.xlsx 형태로 다운로드 파일명을 설정합니다.

클라이언트 사이드 다운로드: 브라우저 메모리에 있는 JSON 데이터를 다시 SheetJS를 통해 엑셀 바이너리 블롭(Blob)으로 변환한 뒤, HTML5의 <a> 태그의 download 속성을 이용해 사용자 PC로 즉시 다운로드 시킵니다. (이 과정에서도 서버 통신은 없습니다.)

4. 🔒 추가 보안 아키텍처 (Security Enhancements)
사내망(Intranet) 최적화: 외부 인터넷 연결이 끊어진 오프라인 상태이거나, 철저히 통제된 사내망 망분리 PC에서도 웹 브라우저만 켜져 있다면 정상 작동하도록 PWA(Progressive Web App) 형태로 캐싱할 수 있습니다.

난독화 및 접근 제어: 웹 페이지 소스코드 난독화를 진행하고, 필요하다면 사내 IP나 특정 인가된 계정만 웹 페이지에 접근할 수 있도록 앞단에 가벼운 인증(Auth) 로직만 서버리스(Serverless)로 구성합니다.

5. 📈 추후 확장성 (Future Scalability)
현재는 서버 없는 브라우저 단독 처리 구조(Client-side)를 추천하지만, 추후 비즈니스가 확장될 때를 대비한 구조화입니다.

템플릿 확장: 현재 '가차(Gacha)' 양식 하나지만, 향후 'A사', 'B사' 등 여러 거래처의 양식이 추가될 경우, 파싱 로직(Step 2)만 별도의 모듈(Strategy Pattern)로 분리해두면 UI 변경 없이 무한히 확장 가능합니다.

통계 데이터베이스 연동 (선택): 추후 '월별 처리 건수'나 '총 주문 금액' 등을 저장해야 한다면, 민감한 정보(이름, 주소, 전화번호)는 제외하고 엑셀의 '수량'과 '상품명' 같은 통계성 데이터만 비동기(AJAX)로 사내 백엔드 서버로 전송하여 DB에 적재하는 하이브리드 방식으로 진화할 수 있습니다.