# RIE ERP — 발주·송장 대조 시스템 사용 가이드 (use.md)

본 시스템은 지점별 **발주서(Purchase Order)**와 **송장(Invoice)** Excel 파일을 자동으로 분석하고 대조하여 수량 및 품목 일치 여부를 결과로 보여주는 업무 자동화 도구입니다.

---

## 1. 주요 기능 및 특징

*   **자동 대조 시스템**: 지점명과 날짜(예: 20251001)를 기준으로 발주서와 송장을 자동으로 매칭합니다.
*   **동적 엑셀 파싱**: 고정된 양식뿐만 아니라 '제품명', '바코드', '수량' 등 키워드를 탐색하여 다양한 엑셀 레이아웃을 지원합니다.
*   **상품 매칭 로직**: 바코드(1순위) 및 정규화된 상품명(2순위)을 사용하여 정확하게 상품을 대조합니다.
*   **데이터베이스 관리**: 모든 대조 결과는 SQLite DB에 저장되어 누적 관리됩니다.
*   **웹 대시보드**: 누적된 데이터를 검색하거나, 실시간으로 파일을 업로드하여 결과를 확인할 수 있는 현대적인 웹 UI를 제공합니다.

---

## 2. 폴더 구조 및 준비사항

실행 전 `data` 폴더 내에 다음과 같은 구조로 엑셀 파일이 배치되어 있어야 합니다.

```text
c:/Users/admin/Desktop/rie/
├── data/
│   ├── 발주서/
│   │   └── [지점명]/ (예: 강남점)
│   │       └── [지점명]_발주서_[YYYYMMDD].xlsx
│   └── 송장/
│       └── [지점명]/
│           └── [지점명]_송장_[YYYYMMDD].xlsx
├── web/ (웹 대시보드 구성 요소)
├── process_compare.py (메인 실행 스크립트)
└── use.md (본 문서)
```

---

## 3. 사용 방법

### 방법 1: Python 일괄 처리 (Batch Processing)

터미널에서 명령어를 실행하여 `data` 폴더 내의 모든 파일을 한 번에 처리하고 DB를 업데이트합니다.

1.  **실행**:
    ```powershell
    python process_compare.py
    ```
2.  **동작**:
    *   `data/발주서`와 `data/송장` 폴더를 스캔합니다.
    *   지점명과 파일명 내의 날짜(8자리 숫자)가 일치하는 쌍을 찾습니다.
    *   이미 처리된 결과가 DB에 있다면 중복 처리를 방지하기 위해 건너뜁니다.
    *   대조 결과를 `data/database.sqlite`에 저장하고, 웹 UI용 `web/comparisons_data.json` 파일을 생성/업데이트합니다.

### 방법 2: 웹 대시보드 사용 (Interactive UI)

브라우저를 통해 시각적으로 데이터를 조회하거나 개별 파일을 직접 대조합니다.

1.  **실행**: `web/index.html` 파일을 브라우저(Chrome, Edge 등)로 엽니다.
2.  **데이터 조회**: '데이터베이스 조회' 탭에서 과거의 모든 대조 결과를 지점별/상태별로 필터링하여 검색할 수 있습니다.
3.  **실시간 대조**:
    *   사이드바에서 지점을 선택합니다.
    *   해당 지점의 발주서와 송장 파일을 화면에 **드래그 앤 드롭**하거나 클릭하여 업로드합니다.
    *   '발주·송장 대조 실행' 버튼을 눌러 즉시 결과를 확인합니다.
    *   *주의: 웹에서 직접 실행한 결과는 브라우저 메모리에서만 동작하며, 서버 DB에는 자동으로 저장되지 않습니다. (보안 목적)*

---

## 4. 상세 대조 로직 (Comparison Logic)

*   **상태 값 (Status)**:
    *   ✅ `ok`: 발주서와 송장의 상품 및 수량이 일치함.
    *   ❌ `miss`: 발주서에는 있으나 송장에는 없는 품목.
    *   ➕ `extra`: 송장에는 있으나 발주서에는 없는 품목.
*   **매칭 우선순위**:
    1.  **바코드 일치**: 엑셀 내 바코드 값이 정확히 일치하는지 먼저 확인합니다.
    2.  **상품명 일치**: 공백을 제거하고 소문자로 변환한 상품명이 동일한지 확인합니다.

---

## 5. 트러블슈팅

*   **"Missing '발주서' or '송장' folder"**: `data` 폴더 위치가 프로젝트 루트에 정확히 있는지 확인하세요.
*   **날짜 인식 실패**: 파일명에 반드시 `20251001`과 같이 8자리 숫자가 포함되어야 자동 매칭됩니다.
*   **헤더 인식 오류**: 엑셀 파일 상단부(15행 이내)에 '제품명', '바코드', '수량' 또는 ERP용 헤더(NM_ITEM, CD_ITEM)가 포함되어 있는지 확인하세요.
*   **데이터 미표시**: Python 스크립트를 한 번이라도 실행해야 `web/comparisons_data.json`이 생성되어 웹 화면에 데이터가 나타납니다.

---
*Last Updated: 2026-04-17*
